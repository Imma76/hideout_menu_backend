PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/hideout_menu
# Comma-separated list of origins allowed to call this API (the frontend and
# admin dashboard). Any http(s)://localhost or 127.0.0.1 origin is always
# allowed regardless of this list (see main.ts) — this is only for deployed
# origins. Production (set directly on Heroku, not from this file):
# https://hideout-six.vercel.app,https://hideout-admin.vercel.app,https://www.thehideoutrestroandlakesidelounge.com,https://thehideoutrestroandlakesidelounge.com
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
